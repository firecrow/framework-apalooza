{
  "name": "framework-apaloza",
  "type": "module",
  "version": "1.0.0",
  "dependencies": {
    "@types/react": "^18.3.2",
    "@types/react-dom": "^18.3.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "svelte": "^4.2.17",
    "ts-loader": "^9.5.1",
    "typescript": "^5.4.5",
    "webpack": "^5.91.0"
  },
  "scripts": {
    "build-react": "tsc -p react.tsconfig.json",
    "build-svelte": "vite build",
    "build": "npm run build-react && webpack --mode production && cp -v src/*.html dist/ && cp -v src/*.css dist/",
    "clean": "rm -Rf dist/* rm -Rf pre-dist/*"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.0",
    "webpack-cli": "^5.1.4"
  }
}
